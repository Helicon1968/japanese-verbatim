---
name: japanese-verbatim
description: 記事・投稿・動画・音声などの外部ソースを、加筆や意訳を排した忠実な日本語マークダウンに変換する。X/Twitter投稿、YouTube、講演動画、PDFなどを「原文訳＋補足＋図解」の三層構成で文書化したいときに使用する。音声を含むソースでは文字起こしの品質ゲート・用語辞書・忠実性チェックを必ず通す。
---

# japanese-verbatim

外部ソースを忠実な日本語マークダウンに変換する。

**この手順の要点は、文書を書くことではなく、素材を欠落なく取り出すことにある。**
自動文字起こしは静かに内容を落とす。落としたことに気づかないまま整形すると、
見た目の整った不完全な文書ができあがる。だから先にゲートを通す。

以下の `$SK` は `~/.claude/skills/japanese-verbatim` を指す。

## 0. 入力の確定（勝手に始めないこと）

**変換対象が指定されていない場合は、必ずユーザーに確認してから始める。**
引数なしで呼ばれたとき、カレントフォルダのファイルを推測で解析し始めてはならない。
確認するのは次の3つ。

1. **何を変換するか** — ファイルパス、またはURL
2. **どこに出力するか** — 既定はカレントフォルダ
3. **成果物の名前** — 日本語で付ける（拡張子なし）。下記 0-1

確定したら作業領域を作る。`--source` は必須で、指定しないと start は失敗する。

```bash
python $SK/scripts/workspace.py start --source <ファイルかURL> --name <日本語の名前>
```

### 0-1. 名前の付け方

**成果物名もフォルダ名も日本語にする。英語のスラッグにしない。**
中身が日本語の文書なのに、ファイル名だけ英語だと一覧で中身が分からなくなる。

| 対象 | 付け方 | 例 |
|---|---|---|
| 成果物名（`--name`） | 内容が分かる日本語の題名 | `ハーネス・エンジニアリング_完全ガイド` |
| 出力先フォルダ | **短くする。** 題名をそのまま使わない | `ハーネス` |

- 区切りは `_` か `・`。**空白を入れない**（コマンドで毎回引用符が要る）
- Windows で使えない文字（`\ / : * ? " < > |`）を避ける
- 製品名・固有名詞はラテン文字のままでよい（`Cloudflare導入メモ`、`ClaudeCode徹底解説`）

`--name` は `<名前>.md` / `<名前>.exempt.txt` / `<名前>.assets` の三点に使われる。
日本語でも問題なく動く（`workspace.py` は文字種の検査も変換もしていない）。

**後から改名するときは、ファイル名だけ変えると壊れる。** 本文中の
`<名前>.assets/...` の画像リンクと、`.jv/audit/manifest.json` の `name` を
同時に直すこと。`name` がずれると `workspace.py` が成果物を認識しなくなり、
`status` が成果物を「非成果物」として報告する。

### 0-2. ソースが最初から日本語のとき

**原文訳は成立しない。** 訳すものがないのだから、契約どおり原文訳ブロックを作れば
引用記法で囲んだ**原文の全文複製**ができあがるだけで、変換の実体がない。
これは成果物ではなく、他人の文章の複製である。そのまま進めてはならない。

**何を作るかをユーザーに確認する。** よくあるのは次の3つ。

| 形 | 中身 |
|---|---|
| **要点の抽出** | 事実・手順・設定値を抜き出して参照メモに組み直す。原文の文章は再現しない |
| **構成の分析** | 技術内容ではなく書き方を対象にする。章立て、用語の開き方、導線の作り方 |
| **記述の検証** | 書かれている内容を一次資料と突き合わせ、ずれがあれば指摘する |

**原文訳ブロック以外の契約はすべてそのまま適用する。** 日本語だからといって
省いてよい手順はひとつもない。とくに次の2つは、このスキルを使う意味そのものなので
必ずやる。

- **📝補足を必ず付ける。** 原文にない背景・前提・落とし穴を足すのが価値の中心である。
  抽出しただけの要約なら、このスキルを通す必要がない。
  **どこからが自分の記述かを節で分ける**（原文の主張と補足を混ぜない）
- **出典画像は通常どおり組み込む**（手順1-2）。情報を持つ画像は元画像を貼り、
  装飾画像は一行の説明に留める。`get_page_text` は画像を落とすので、
  **DOM から列挙して1点ずつ中身を確認する**。本文に書かれていない情報が
  図にだけ入っていることがある

目次・出典の明示・文体・後始末も変わらない。

**ゲート2と3は原文訳ブロックを前提にしているため RED を出す。**
これは意図した逸脱であり、`<名前>.exempt.txt` に理由を記録する。
黙って通さない。偽の原文訳ブロックを置いて通すのはもっと悪い。

### 0-3. どの版を作るか（言語を問わず必ず聞く）

**素材の分量は事前に分からない。長い素材では、深さの違う版を使い分けたくなる。**
着手する前に、どれを作るかをユーザーに聞く。**複数選択できる形で聞くこと**
（片方だけと決めつけない）。

**複数選択にするなら、選択肢は互いに独立させる。**
「詳細版のみ」のような**排他を含む選択肢を複数選択に混ぜない**。
「詳細版のみ」と「要点版も作る」を並べると両方にチェックが入り、意味が取れなくなる
（実際にそうなった）。並べるのは「詳細版」「要点版」のように、
**どれを選んでも、どう組み合わせても成立するもの**にする。

推奨を伝えたいときは選択肢の名前ではなく説明文に書く。
「元記事が短いので差が出にくい」といった判断材料は説明側に置けばよい。

| 版 | 中身 | 向いている場面 |
|---|---|---|
| **詳細版** | 全節を網羅する。原文のASCII図はそのまま再現し、出典画像も貼る。要所は原文の引用と訳を並べる。各節に📝補足 | 手元で参照し続けるもの |
| **要点版** | 主張だけを短くまとめる。図は要点に関わるものだけ | 通読して全体像を掴むもの |

**両方を出すなら、成果物名を2つ登録する。**

```bash
python $SK/scripts/workspace.py start --source <…> --name <詳細版の名前> --also <要点版の名前>
python $SK/scripts/workspace.py name --add <名前>    # 後から足す
```

登録していない `.md` を出力先に置くと、`status` が**非成果物として報告する**。
版を増やすかどうかは分量を見てから決めてよいので、後から `name --add` で足せる。

**詳細版でも、原文の地の文を一文ずつ置き換えた全文版は作らない。**
それは成果物ではなく原文の複製であり、読者にとって原文の代わりになる。
残すのは事実・手順・判断基準・図であって、著者の文章そのものではない。
言い回しが要点になる箇所（対句、節を締める箴言、定義文）は**引用として残す**。

この線引きは原文の言語と関係ない。英語の素材でも同じである。
ASCII図・コード・設定値は「情報そのもの」なので、そのまま再現してよい。

## 作業場所の規約（厳守）

```
<出力先>/
  <名前>.md               ← 成果物
  <名前>.exempt.txt       ← 成果物（忠実性チェックの判断記録）
  .jv/
    work/    中間ファイル。**その場で書くスクリプトもここに置く**
    audit/   出典の生データ・検証結果（後から再検証するために残す）
    cache/   再実行キャッシュ
```

**出力先の直下に置いてよいのは成果物だけ。** 切り出した音声、途中の文字起こし、
確認用に抜いたフレーム、デバッグ用のスクリプト — すべて `.jv/work/` に入れる。
各スクリプトの既定値はそうなっているので、出力先を上書き指定しないこと。

作業が終わったら必ず後始末する（手順9）。

## 成果物の契約

各セクションを次の3ブロックで構成する。ブロックの役割を混ぜない。

| ブロック | 内容 | 規則 |
|---|---|---|
| **原文訳** | ソースの内容の日本語化 | 加筆・意訳をしない。引用記法（`>`）で囲む。**全文版は作らない**（0-3） |
| **画面** / **原文ブロック** | 画面表示・コード・設定など | ソースに現れた形のまま。コードは原語のまま残す |
| **📝 補足** | 訳者による背景説明 | 原文に含まれないことを明示する |

### 図版の扱い（重要）

**原文の図は元画像を貼る。訳者が足す図は Mermaid で描く。この2つを混ぜない。**

| 元素材 | 扱い |
|---|---|
| 原文が持つASCII図 | コードフェンスでそのまま再現する。改変しない |
| 原文の画像で**情報を持つもの**（図解・スクリーンショット） | **必ず元画像を貼る**（手順1-2）。Mermaidで描き直さない |
| 原文の**装飾画像**（タイトル画像・ヘッダー等） | 一行で内容を説明するだけにする。取得しない |
| **動画の画面**（設定ファイル・コマンド・ツールの出力） | 該当時刻のフレームを貼る（手順5-2）。選定基準は5-3 |
| 訳者が理解を助けるために足す図 | **Mermaid で描く。**「訳者による作図」と明記する |

原文の図を Mermaid で描き直してはならない。描き直すと情報が落ちるうえ、
ゲート2は `![](...)` の数で取りこぼしを検査するため、Mermaid では通らない。
Mermaid は原文に存在しない図を訳者が足すときだけ使う。

**罫線文字（`┌ ─ │ └ ├`等）で自分で作図してはならない。**
画像をASCIIアートで再現するのは最も避けるべき形で、原文より分かりにくくなる。
訳者が描く図は必ず Mermaid にする。原文のASCII図をそのまま再現する場合のみ、
コードフェンスに罫線が入ってよい。

### 画像を置く位置

**画像を「原文訳」ブロックの中に置いてはならない。**

ブロックは次の見出しまで続く。`### 原文訳` の直下に画像を置くと、
画像のパスまでゲート3の検査対象になり、
`assets/02-2-1199x675.jpg` のような文字列が「出典に見つからないトークン」として
報告される。出典の原文にそんな文字列は無いのだから当然である。

```
## N. 節の見出し

（要点）

![出典の図：…](<名前>.assets/01-….jpg)   ← ここに置く

### 原文訳

> 引用と訳

### 📝 補足

（訳者の記述）
```

**補足ブロックの中へ逃がすのは誤りである。** ゲート3は通るようになるが、
補足は訳者の記述を置く場所であって、出典の素材を入れる場所ではない。
画像は原文訳ブロックの**手前**に置く。

同じ理由で、原文のコードブロック（ASCII図・設定例）も原文訳ブロックには入れない。
「画面」「原文ブロック」の見出しを立てるか、原文訳の手前に置く。

### 文体

**ブロックごとに文体を固定する。同じ文書の中で揺らさない。**

| ブロック | 文体 |
|---|---|
| **原文訳** | 敬体（です・ます）。原文が命令形なら「〜してください」 |
| **📝 補足**・付録・出典 | 常体（である）。訳者の記述であることが文体で分かる |

原文訳を敬体にするのは、読者に語りかける原文の調子を保つため。
補足を常体にするのは、原文と訳者の声を文体で区別するため。

- **一文の長さは原文に合わせる。** 短い文を畳みかける原文を、読みやすさのために
  つなげてはならない。段落の区切りも原文のまま保つ

**例外**: 原文が短文を並べて畳みかける箇所（失敗の列挙、対句、節を締める箴言）では、
敬体が調子を壊す。そこは常体のままでよい。ただし**その段落の中では揃える**。

```
The agent did not know which files mattered.   → エージェントは、どのファイルが重要なのかを知らなかった。
Store raw history for auditability.            → 生の履歴は監査可能性のために保存する。
```

文体を揃えることは目的ではなく、原文の調子を保つための手段である。
両者が衝突したら原文の調子を優先し、なぜそうしたかを📝補足に書く。
段落をまたいで無自覚に混ざっているなら、それは推敲の漏れである。

**原文の1文を訳で2文に割るときは、両方の文末を同じ文体で閉じる。**
ゲート4が出す「原文訳で常体が1文だけ孤立」は、**実測でこれが最多の原因**である
（3回の実行で連続して出た）。長い1文を割ると、前半をつい体言止めや常体で閉じてしまう。

```
that we want to address, that's why we define our own agentic stack …

  誤  こうした課題に取り組みたい。だからこそ、…を定義しました。
  正  こうした課題に取り組みたいと考えました。だからこそ、…を定義したのです。

I probably would have called a transformer a … computer instead of attention is all you need.

  誤  私なら…と呼んだでしょう。`Attention Is All You Need` ではなく。
  正  `Attention Is All You Need` ではなく、私なら…と呼んだでしょう。
```

後者のように**語順を変えて1文に戻す**ほうが、訳として自然になることが多い。
2文に割るのは、原文が実際に2文であるときに限るとよい。

### そのほかの規則

- 長い文書（目安200行超）には**冒頭に目次を置く**。節番号・見出し・一言要約の表にする
- 図（Mermaid等）は原文にない限り「訳者による補足」と冒頭で明記する
- 冒頭に出典・取得日時・取得方法を書く。**素材が複数のつなぎ合わせなら、その区切りも書く**（手順5-4）
- 音声ソースでは**精度に関する付録を必ず添付**する
- 判断に迷った箇所は「〔聞き取り不明瞭〕」等で明示し、黙って補完しない
- 補足を要しないセクション（告知のみ等）には `<!-- contract:exempt -->` を置く。
  黙って省略せず、意図的な判断として残す

## 全体の流れ

```
0.入力確定 ─▶ 取得 ─▶ 文字起こし ─▶ [ゲート1] ─▶ 用語辞書 ─▶ 文書化 ─▶ [ゲート2][ゲート3] ─▶ 付録 ─▶ 後始末
 (確認)              (キャッシュ)   verify_      glossary    (判断)     verify_doc       make_    workspace
                                   transcript                          verify_fidelity  appendix  finish
                                        │                                   │
                                        └─── RED なら設定を直して再実行 ────┘
```

新しく見つけた誤認識は、**必ず `glossary.py add` で辞書に戻す。**
これを飛ばすと、同じ誤りに毎回出会う。

## 1. 取得

| ソース | 方法 |
|---|---|
| **成果物の出典にするWeb記事**（x.com / Qiita / ブログ等） | **ブラウザペイン**（`navigate` → `get_page_text` → DOM で画像を列挙） |
| 検証や裏取りのために読む参照先 | WebFetch でよい |
| ローカル動画・音声 | 下記2へ |

**成果物の出典には WebFetch を使わない。** 理由は2つある。

- **生テキストが取れない。** WebFetch が返すのは小型モデルがページを読んで
  答えた要約であって、ページそのものではない。`raw.txt` はゲート3の照合元なので、
  要約を置くと「出典に無い」の判定が当てにならなくなる
- **画像を列挙できない。** DOM を見られないため、下記の `[[IMAGE]]` 記録も
  「図にしか無い情報」の発見もできない。**x.com 以外でも同じである**

逆に、**裏取りのために読むだけの参照先には WebFetch が向く**。
ページを丸ごと持ってくる必要がなく、聞きたいことを絞って投げられる。
「記述の検証」で一次資料に当たる場合などはこちら（実例: typesafe.ai のドキュメント6本）。

**取得した生テキストは `.jv/audit/raw.txt` に保存する。**
忠実性チェック（ゲート3）の照合元になる。

**出典に画像がある場合は、必ず次の形で raw.txt に記録する。**

```
[[IMAGE <メディアID>]] 画像の内容の説明（装飾的なものは decorative を含める）
```

X/Twitter のメディアIDは投稿DOM中の `pbs.twimg.com/media/<ID>` から取れる。
記録しておけば `fetch_images.py` が取得計画と参照タグを生成する。

### 1-2. 出典画像の組み込み

```bash
python $SK/scripts/fetch_images.py --raw .jv/audit/raw.txt --name <名前>          # 計画の確認
python $SK/scripts/fetch_images.py --raw .jv/audit/raw.txt --name <名前> --apply  # 取得
```

保存先は `<名前>.assets/` で、**成果物の一部**として残す（`.jv/work/` ではない）。
出力された `![...](...)` を本文の該当箇所に貼る。

扱いの区別は「図版の扱い（重要）」のとおり。情報を持つ画像は必ず元画像を貼り、
装飾画像は一行の説明に留める。ASCIIアートでもMermaidでも描き起こさない。

**ダウンロードはユーザーの承認が要る操作である。** `--apply` を実行する前に、
点数と合計サイズを示して確認を取ること。

## 2. 文字起こし

```bash
# 速度を実測してモデルを選ぶ（推測しない）
uv run --no-project --python 3.12 --with faster-whisper --with imageio-ffmpeg \
    python $SK/scripts/transcribe.py <動画> --bench

# 本実行（.jv/work/ に出力、.jv/cache/ にキャッシュ）
uv run --no-project --python 3.12 --with faster-whisper --with imageio-ffmpeg \
    python $SK/scripts/transcribe.py <動画> --model small --beam 5 --no-hotwords
```

`--python 3.12` の明示は必須（省略すると tokenizers のビルドに落ちて失敗する）。
ffmpeg のパスは固定せず `imageio_ffmpeg.get_ffmpeg_exe()` で毎回解決する。

**`--no-hotwords` を外さないこと。** `transcribe.py` は既定で辞書から hotwords を
注入するが、これは反復ループを誘発してその間の発話を丸ごと落とす。用語は手順4の
事後置換で回復する。実測と根拠は `references/asr-playbook.md` の失敗モード3。
そのほかの設定の根拠と失敗モードも同じファイルにある。

## 3. ゲート1：文字起こしの品質（スキップ禁止）

```bash
python $SK/scripts/verify_transcript.py .jv/work/transcript.json --audio <音声>
```

終了コード **0=GREEN / 1=RED / 2=検証不能**。**RED と 2 では先に進まない。**
検証できないことは合格ではない。結果は `.jv/audit/gate.json` に残る。

発話の欠落は、ギャップの存在ではなく**音量の実測**で判定する。
反復ハルシネーションは再実行では直らないので、該当箇所を整理してから
`--repetition warn` で通す。

## 4. 用語辞書

```bash
python $SK/scripts/glossary.py apply .jv/work/transcript.txt \
       -o .jv/audit/fixed.txt --report .jv/audit/gloss.json
python $SK/scripts/glossary.py add --wrong "CloudMD" --right "CLAUDE.md" --note "頻出"
```

**辞書は事後の置換にだけ使う。hotwords として事前に渡さない。**
hotwords は反復ループを誘発し、その間の発話を丸ごと落とす。手順2で
`--no-hotwords` を付けるのはこのため（実測は asr-playbook の失敗モード3）。
`glossary.py hotwords` は残してあるが、比較検証のとき以外は使わない。

**照合元（fixed.txt）は `work/` ではなく `audit/` に置く。**
`work/` は finish で消えるため、そこに置くとゲート3を二度と再実行できなくなる。

`review` 扱いの項目は自動置換しない。文脈を見て判断する。
辞書に入れてよいのは**語・短い固有名詞まで**（4語超は拒否される）。

## 5. 画面付きソースの補強

**音声で曖昧だった技術用語は、ほぼ必ず画面に映っている。**
焼き込み字幕も自動生成であることが多く、同種の誤りを含む。正解として扱わないこと。

### 5-1. 候補を出して読む

```bash
uv run --no-project --python 3.12 --with imageio-ffmpeg \
    python $SK/scripts/grab_frames.py <動画>
```

抜き出したフレームは `.jv/work/frames/` に入る（finish で消える）。
**1枚ずつ読んで内容を確かめる。** 用語の照合はここで済ませる。

### 5-2. 採用した画面を成果物に入れる

```bash
uv run --no-project --python 3.12 --with imageio-ffmpeg \
    python $SK/scripts/grab_frames.py <動画> \
        --at <秒,秒,…> --assets <名前> --crop-bottom 12
```

`<名前>.assets/` に入り、`raw.txt` 用の `[[IMAGE]]` 行と本文用の参照が出力される。
`[[IMAGE]]` を `raw.txt` に追記すれば、Web記事と同じ取りこぼし検査（ゲート2）が
動画でも効く。**説明の `〔〕` を埋めること。ゲート2は点数しか数えない。**

**`--assets` は `--at` を必須にしてある。** シーン検出が返すのは画面が切り替わった
瞬間であって、話者がその画面を説明している時刻ではない。候補をそのまま貼ると
図と本文が数十秒ずれる。自分で選んだ秒数を指定する。

### 5-3. 何を貼るか（選定基準）

**画面上の文字が情報そのものになっているフレームだけを貼る。**

| 貼る | 貼らない |
|---|---|
| 設定ファイル・コード | 話者のアップ |
| コマンド一覧・メニュー | 暗転・トランジション |
| ツールの出力・エラー | ブラウザのクロームだけ |
| 図解スライド（下記の但し書きを読むこと） | スクロールの途中・重複 |

実測では、62分の講演から検出された60枚のうち、画面上の文字が情報になっていたのは
6割ほどだった。**全部貼らない。** 十数枚に絞る。

**切り取りを使う。** 焼き込み字幕をそのまま貼ると、誤った字幕を根拠資料として
貼ることになる（「字幕を正解として扱わない」という規約に自分で反する）。
話者のワイプも情報ではない。`--crop-bottom` / `--crop-right` で落とす。
ただし**切りすぎは欠落である。** 右端を詰めると画面の情報まで切れることがあるので、
結果を目で見て確かめる。

#### 切り取り幅は一律にしない

**幅を1つ決めて全部に当てるとやられる。** 字幕は下端にあるが、
**スライドの下端に情報がある回がある**。実測した例。

| 画面 | 下端にあったもの | 使った幅 |
|---|---|---|
| 結論が赤字で最下行にあるスライド | 「変わったのは pre-norm だけ」という本文そのもの | **1%** |
| 字幕が2行になる回 | 字幕だけ | 14%〜18% |
| 題名と講演者だけ残したいスライド | 映画のスチルと論文の図 | **67%** |

手順は「**先に下端に何があるかを見てから幅を決める**」。候補を一覧して
（5-1）、下端に情報がある画面を洗い出し、**必要なら画面ごとに別の幅で抜き直す**。
`--at` は複数回に分けて実行できるので、幅の違う組を順に出せばよい。

**切り取りで落ちた情報は、本文に文字で補う。** 落としたまま貼らない。
実測では次の2つを補った。

- 最下部の数式（`Attention(Q,K,V) = softmax(QK^T/√d_k)V`）
- **画像の出典表記**（`CC-BY-SA by …`、`slide from …`）

**出典表記は字幕と同じ帯にあることが多い。** 切ると一緒に落ちる。
**表記を落としたまま貼るのは、出所不明の図を貼るのと同じである。**
落ちたら本文に書き写す。

#### 権利上の線引き

**判断は3段ある。** 「講演者が作ったか否か」だけでは足りない。
実際に多いのは、**講演者のスライドの中に他者の図や投稿が入っている**形である。

| 画面の中身 | 判断 |
|---|---|
| 設定ファイル、コマンド、ツールの出力 | **貼ってよい。**「その製品がそう表示する」という事実である |
| 講演者が作った文・一覧・図・コード | **貼ってよい**（下記の但し書きを読むこと） |
| 講演者の構成の中に他者の図・投稿が入る | **加工して貼る。** 切り取りで他者の部分を落とせるなら落とす。落とせないなら要旨に替える |
| 画面の全体が他者の成果物の複製（投稿のスクリーンショット等） | **貼らない。** 要旨を訳し、引用元を示す |
| 自分の録画、社内資料、自由なライセンスの動画 | 制約なし |

**切り取りは権利判断の道具でもある。** 実測では、題名・講演者・講義名だけを残して
下67%を落とし、映画のスチルと論文の図を外した。**出典を示すという目的には足りる。**

有料・無償の線引きも変わらない。**有料の講義でスライドを貼ると、成果物が
講義の代替物になる。** その場合は図の内容を文章と表に落とし、画面は設定・出力に限る。
無償公開の講義（大学の公開講義など）なら図解スライドも採ってよい。

**どちらに寄せたかは `<名前>.exempt.txt` に書く。** 判断そのものより、
判断の理由が後から要る。

### 5-4. 素材が1本かどうかを確かめる

**長い動画は、複数の素材のつなぎ合わせであることがある。**
気づかずに1本として書くと、**冒頭の出典欄が嘘になる**。訳や補足を正しく書いても、
出典が誤っていれば成果物として成立しない。

**実測**: 68分の動画が、対面の基調講演（〜22:09）＋別の講義の断片（〜24:26）＋
Stanford CS25 の講義（24:26〜）の3本だった。X から取得したもので、
つないだ順番は時系列ですらなかった（後半のほうが1か月早い）。

兆候は映像と音声の両方に出る。**どれか1つでも見つけたら疑う。**

| どこに出るか | 兆候 |
|---|---|
| **映像** | 画作りが変わる（対面撮影 ⇄ 画面共有、ワイプの有無、解像度感） |
| **映像** | **タイトルスライドが冒頭以外に出る**。これがいちばん確実 |
| **音声** | 司会の締めや拍手が途中に入る（「thank you so much」等） |
| **音声** | 自己紹介が2回出る。話者が変わる。音質・残響が変わる |
| **内容** | 同じ話題を2回立ち上げる。前半の結びのあとに新しい導入が来る |

確かめ方は2つで足りる。

- **シーン検出の候補を時系列で一覧して見る**（5-1）。画作りの変化は縮小しても分かる
- **文字起こしを締めの語で拾う**。`thank you` `applause` `so today` `let me introduce`

見つけたら、次の3つをやる。

1. **冒頭の出典欄に区切りを書く**（何分から何分が何なのか）
2. **判断の根拠を `<名前>.exempt.txt` に残す**。後から見て再確認できるようにする
3. **節構成を区間で分ける**。連続した1本のように並べない

**疑わしいが確定できないときは、確定できないと書く。** 素材の由来は推測で埋めない。

## 6. 文書化

ここからはモデルの判断領域。セクションの切り方、補足に何を書くか、
どの図を入れるかは型にはめない。ソースごとの事情（編集によるカット、
質疑応答の混在、話者の脱線）に応じて構成する。

成果物は `<出力先>/<名前>.md` に書く。作業用のファイルは `.jv/work/` に置く。

## 7. ゲート2・3・4：成果物の検査

```bash
python $SK/scripts/verify_doc.py <名前>.md --require-appendix --raw .jv/audit/raw.txt
python $SK/scripts/verify_fidelity.py <名前>.md --source .jv/audit/fixed.txt \
       --ignore-file <名前>.exempt.txt
python $SK/scripts/verify_style.py <名前>.md
```

- **ゲート2**は三層構成・出典・図の帰属・目次・Mermaid構文・罫線作図・
  出典画像の取りこぼしを検査する
- **ゲート3**は原文訳ブロックの識別子が出典に存在するかを照合する。
  日本語訳文そのものの忠実性は検査できない。検査できるのは固有名詞・
  識別子の出所だけだが、捏造がもっとも危険なのはそこ
- **ゲート4**（文体）は「文体」の規約に対する揺れを見る。常体そのものは
  叱らない（例外があるため）。叱るのは**補足に混ざった敬体**と、
  **原文訳で1文だけ孤立した常体**の2つ。常体の連続は例外の候補として
  INFO に出るので、`--show-runs` で中身を確認し、原文が畳みかける箇所か
  どうかを自分で判断する

ゲート3が指摘した語のうち、正当な正規化（発話 "control X" → `Ctrl+X` 等）は
`<名前>.exempt.txt` に**理由つきで記録する**。コマンドラインの一時的なフラグに
逃がさない。判断を残しておけば後から見直せる。

## 8. 付録の生成

```bash
python $SK/scripts/make_appendix.py --gate .jv/audit/gate.json \
       --glossary .jv/audit/gloss.json --exempt-file <名前>.exempt.txt \
       --method "faster-whisper small / beam=5" -o .jv/work/appendix.md
```

手で書くと都合の悪い項目が落ちる。機械が書けば落ちない。
生成した内容は成果物の末尾に取り込む。

## 9. 後始末（必須）

```bash
python $SK/scripts/workspace.py status          # 何が残っているか点検
python $SK/scripts/workspace.py finish          # 消す対象の確認（dry run）
python $SK/scripts/workspace.py finish --apply  # 実行
```

`.jv/audit/` と `.jv/cache/` は既定で残る。前者は後から再検証するため、
後者は再実行を即座に終わらせるため。完全に片付けるなら `--purge-cache`。

`status` は出力先に残った非成果物も報告する。**報告が出たら規約違反である。**
`.jv/` の外にあるものは finish では削除しないので、内容を確認して手で処理する。

**消せないものがあっても finish は落ちない。** Dropbox や検索インデクサが
直前まで書いていたディレクトリを掴んでいることがある。そのときは消せなかったものを
一覧で報告する。**空のディレクトリだけが残るなら実害はない**（終了コード0）。
**ファイルが残った場合は終了コード1**なので、内容を確認して手で処理する。

**ユーザーに完了を報告する前に、必ず `status` を実行して出力先が
成果物だけになっていることを確認する。**

## 公開時の注意

スキル本体（スクリプト・規約・辞書）は公開してよい。
**生成した訳文・文字起こしは二次的著作物であり公開できない。**
README やテストに実例を同梱しないこと。ffmpeg バイナリを同梱しないこと。
